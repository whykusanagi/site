# Favicon lives in R2 (matches the old Worker's redirect), avoids a per-page <link>.
/favicon.ico  https://s3.whykusanagi.xyz/art/idol_signature/idol-sig.png  301

# Art gallery temporarily offline while the set is re-curated (2026-07-11).
# Restore: delete these two lines + re-add the "Art Gallery" nav links.
/art       /  302
/art.html  /  302

# Union Raid calculator retired 2026-07-11 (no longer used).
/union_calc       /tools.html  301
/union_calc.html  /tools.html  301

# Sitemap moved to the standard path 2026-08-08. ai-index.xml was never
# discoverable: nonstandard name, no robots.txt Sitemap: line, no <link>.
/ai-index.xml  /sitemap.xml  301
