# Development & Documentation (local reference only)
CELESTE_*.md
CLOUDFLARE_WORKER_SETUP.md

# Node/npm
node_modules/
package-lock.json
npm-debug.log

# OS
.DS_Store
._*
.AppleDouble
.Spotlight-V100
.Trashes
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment & Secrets
.env
.env.*
.env.local
.env.*.local
.env.docker
.secrets/

# Temporary Files
*.tmp
CLAUDE_temp.md

# Video files (upload to R2, don't commit to git)
*.mp4
*.webm
assets/animations/*.mp4
assets/animations/*.webm
assets/animations/website_background_video_*.mp4

# 3D Model files (upload to R2, don't commit to git - large binary files)
models/
*.vrm

# Corrupted Theme Package (local development, will be extracted to separate repo)
/corrupted-theme/

# GitHub Projects (cloned repos for reference, don't commit to main site)
github_projects/

# Celeste CLI local data
.celeste/

# Superpowers brainstorm session artifacts (visual companion mockups)
.superpowers/

# --- Public-repo hygiene (do not commit internal/agent/local files) ---
# Internal engineering docs: specs, plans, dependency charts, ops notes.
# These live in CelesteOps, never in the public repo. (tools/docs/ is kept.)
/docs/
# Agent guardrails / local context (align with ai_development template; kept local)
CLAUDE.md
CLAUDE.local.md
.grimoire
.claude/
# NSFW / working assets and internal decks
**/headshots/
celeste-ops-presentation.html
# Only root README.md may be committed; every other markdown stays out (docs -> CelesteOps)
*.md
!/README.md
# Agent config (kept local, like CLAUDE.md)
.cursorrules
